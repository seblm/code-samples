name: Java CI with Maven

on:
  push:
    branches: [ main ]
    paths:
      - 'all/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'all/**'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -B package --file all/pom.xml

